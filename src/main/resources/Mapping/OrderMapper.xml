<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pgk.delivery.Order.Mapper.OrderMapper">
    <insert id="addOrder" parameterType="com.pgk.delivery.Order.Pojo.Order">


INSERT INTO `order` (
	orderId,
	orderBuyerId,
	orderSellerId,
	orderRiderId,
	orderState,
	orderBuyerTime,
	orderSellerTime,
	orderRiderTime
)
VALUES
	('', #{orderBuyerId}, #{orderSellerId}, #{orderRiderId},#{orderState},#{orderBuyerTime},#{orderSellerTime},#{orderRiderTime})

    </insert>
</mapper>